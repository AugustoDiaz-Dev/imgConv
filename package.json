{
  "name": "imgconv",
  "version": "1.0.0",
  "private": true,
  "description": "Image batch converter - core library and Electron desktop app",
  "scripts": {
    "build:core": "npm run build -w packages/core",
    "build:desktop": "npm run build -w packages/desktop",
    "dev": "npm run dev -w packages/desktop",
    "build": "npm run build:core && npm run build:desktop"
  },
  "workspaces": [
    "packages/core",
    "packages/desktop"
  ]
}
